name: Docker CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Docker Build
        run: |
          docker build -t jackwhelan/python-microservice-base .
